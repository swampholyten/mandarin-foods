---
import DishCard from "@/components/dish-card";
import { chuancai } from "@/data/sichuan";
---

<div class="flex flex-col justify-center items-center gap-20">
  {chuancai.map((food) => <DishCard food={food} />)}
</div>
